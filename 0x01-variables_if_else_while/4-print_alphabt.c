#include <stdio.h>
/**
 * Main - Prints the alphabet in lowercase, and ignores q and e.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char = letter;

	for (Letter = 'a', letter <= 'z', letter++)
	{
		if (letter != 'e' && letter != 'q')
			putchar(letter);
	}
	putchar('\n')
		return (0);
}
